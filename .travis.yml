language: node_js
node_js: '12'

cache:
  yarn: true
  directories:
    - node_modules

install:
  - yarn --frozen-lockfile --non-interactive

jobs:
  include:
    - stage: 'Lint'
      name: 'Lint'
      script:
        - yarn lint

    - stage: 'Test unit and  e2e'
      name: 'Test'
      before_script:
        - yarn global add codecov
      script:
        - yarn test:unit & yarn test:e2e
      after_script:
        - codecov
